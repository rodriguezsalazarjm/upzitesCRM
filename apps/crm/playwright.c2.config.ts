import { defineConfig } from '@playwright/test';
export default defineConfig({ testDir: './tests/c2', timeout: 90000, expect: { timeout: 15000 }, workers: 1, fullyParallel: false, retries: 0, reporter: 'list', outputDir: '.local-visual/playwright-results', use: { channel: 'chrome', baseURL: 'http://localhost:3101', headless: true, viewport: { width: 1440, height: 1000 }, screenshot: 'only-on-failure', trace: 'retain-on-failure' } });
